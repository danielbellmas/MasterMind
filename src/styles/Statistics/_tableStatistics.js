import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  container: { width: 650, fontSize: "2rem" },
  table: {
    textAlign: "center",
    border: "1px solid black",
  },
}));
