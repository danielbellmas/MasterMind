import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  container: {
    display: "flex",
    justifyContent: "flex-end",
    gridAutoFlow: "row",
    marginTop: "20px",
  },
}));
